# 架构

[English](ARCHITECTURE.md)

本文说明 Math Chords 0.5.x 的运行结构和信任边界。用户功能以 README 为准，
实现规则以 `AGENTS.md` 为准。

## 原则

- 笔记始终是普通 Markdown、LaTeX 和围栏 TikZ 源码。
- 编辑器修改通过 Obsidian 事务提交，并可完整撤销。
- 解析与转换逻辑尽量保持为不依赖 Obsidian API 的纯模块。
- 昂贵工作按需启动、具有明确上限、能够取消，并避开编辑主线程。
- 不支持的 TikZ 明确失败或进入兼容后端，不能生成看似合理但实际错误的图。

## 运行层级

```text
Obsidian 生命周期与设置
├── 公式写作
│   ├── leader 状态机与快捷键 trie
│   ├── 公式和模板面板
│   ├── 公式切换、大括号跳转与环境规划
│   └── 受保护的定界符转换
└── 可选 TikZ 渲染
    ├── 代码块处理与编辑预览
    ├── 渲染协调与有界缓存
    ├── 原创 Rust/WASM 矢量渲染器
    ├── 可选本机 TeX 兼容后端
    └── 安全显示、MathJax 标签与导出
```

`src/main.ts` 负责注册与组织。纯操作位于独立模块，UI 和编辑器适配层调用这些
模块，而不把解析规则堆入入口文件。

## TikZ 后端

默认渲染器位于 `crates/chord-tikz-core`，没有 Cargo 第三方依赖，并编译为小型
WASM。生成模块和原创 Worker 源码压缩进 `main.js`，标准三文件安装已经完整，
无需运行时下载。

一个可复用 Worker 负责初始化和渲染。请求具有源码、时间、内存、展开深度、循环、
采样和输出大小上限；取消或超时会终止 Worker，再继续后续工作。数学标签先输出为
锚点，再由 Obsidian MathJax 渲染。

自动模式让受支持图形使用同一 WASM 实例和缓存。能力分析器把暂时无法忠实复现的
语法交给可用的本机 TeX。后端选择只改变派生结果，不改写笔记。

本机 TeX 仅用于桌面端，并且完全可选。检测覆盖常见发行版、`PATH` 和用户覆盖路径。
进程通过参数数组而非 shell 启动，关闭 shell escape，隐藏控制台窗口，使用经过校验
的临时目录，并限制时间和大小。TeX 仍是复杂解释器，因此该后端只适合可信源码和
正式兼容工作。

## 预览、安全与缓存

阅读视图只调度视口附近的图。编辑预览是 CodeMirror 布局之外的固定浮层。新画面先
离屏完成再原子替换，编辑期间保留上一张成功图。

SVG 使用元素与属性允许列表。脚本、事件处理器、外部链接、外部填充 URL、不安全
raw special，以及非本地箭头、遮罩和裁剪引用都会被拒绝。桌面导出通过系统保存
对话框生成 SVG、PNG、JPEG 或 PDF。

最新请求优先协调器会取消过时工作。内存和 IndexedDB 缓存同时受条目数与字节数
限制。持久缓存把清理元数据与图像正文分开，清理时无需读回所有图。缓存键包含
渲染器版本、WASM 指纹、后端、主题、字体和源码。

## 多语言

英语位于 `src/l10n/locales/en.ts`，其余 71 个语言包以
`scripts/locale-catalog.json` 为来源。生成过程校验完整键覆盖，并把全部语言包压缩
进 `main.js`；启动时只展开当前语言。

简体中文、繁体中文、法语、德语、西班牙语、意大利语、巴西葡萄牙语、俄语、日语和
韩语作为主要语言持续维护完整翻译。其他已声明的 Obsidian 语言同样离线随插件提供；
尚无经过审校的翻译时使用当前英语回退文本，避免显示过期文案，也无需二次下载。

## 构建与外部边界

`src/defaults.ts`、英语语言源、语言目录和 Rust crate 是源输入。生成语言模块、
`shortcuts.yaml`、嵌入 WASM 与 `main.js` 通过生成和校验更新，不能手工修改。
生产包会压缩；发布 CI 执行完整门禁，只发布 `main.js`、`manifest.json` 和
`styles.css`。

Math Chords 不捆绑其他 TikZ 插件、浏览器 TeX 快照、独立 MathJax 或 PDF 渲染器。
MathJax、PDF、编辑器和 CodeMirror API 由 Obsidian 提供；本机 TeX 是明确选择的
可选系统依赖。Rust 渲染器及其组织、安全清洗、缓存、预览和导出层均在本仓库维护。
