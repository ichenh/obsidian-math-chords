# 路线图

[English](ROADMAP.md)

本文记录未来工作及其设计约束。“公式编号和引用”已确定为下一个产品里程碑；其他事项在正式接受实施前不指定目标版本或时间。

## 下一里程碑：公式编号和引用

### 目标

在不改写公式内容、也不让笔记依赖隐藏全局状态的前提下，提供可靠的公式编号与交叉引用。

### 推荐设计

- 以 Markdown 中显式保存的标签作为持久标识；编号属于派生显示层，并随当前文档顺序保持确定性。
- 支持带编号的行间公式、可选自定义标签，以及可通过搜索选择器插入的引用。
- 首先建立文档级索引。跨笔记引用必须显式指定目标笔记，并在插件不可用时仍退化为可读 Markdown。
- 明确提示重复、缺失、已重命名或已删除的标签；遇到歧义时不得猜测目标，也不得静默改写无关笔记。
- 编辑后只重新计算受影响范围；实时预览、阅读视图、自动补全和导出共用同一索引。
- 插入、重命名或更新引用都通过一次编辑器事务完成，一次撤销即可恢复原状态。

### 验收条件

- 源码模式、实时预览、阅读视图、PDF/HTML 导出以及重启 Obsidian 后，公式编号与引用结果一致。
- 插入或重命名标签时，不移动光标、不改动公式内容，也不产生嵌套数学定界符。
- 重复标签和未解析标签提供无障碍、可操作的诊断信息。
- 大型笔记采用增量更新，不因每次按键重新扫描并渲染所有公式。
- 作者未明确启用编号或插入引用前，现有笔记保持不变。

### 本里程碑不包含

- 不自动对整个仓库重新编号。
- 不依赖未公开的 MathJax 内部实现，也不依赖其他 Obsidian 插件。
- 不引入文献管理语义；公式引用保持为专注的 Markdown 功能。

## 文档级 LaTeX 宏

### 目标

允许使用 `\dd` 等简洁命令，同时保持每篇笔记可独立复现，并避免无约束地修改 Obsidian 共享的 MathJax 状态。

它与快捷键展开并不等价：快捷键只在某个位置插入一段完整表达式；文档级宏则让反复
出现的记号拥有一处可见定义，修改该定义即可在整篇笔记中保持一致语义。

定义示例：

```latex
\newcommand{\dd}{\mathop{}\!\mathrm{d}}
```

### 推荐设计

- 使用结构化条目保存宏：命令名称、参数数量、可选默认值和替换内容。
- 提供显式命令，在当前笔记中插入或更新宏前导定义；存在 YAML frontmatter 时，将其放在 frontmatter 之后。
- 定义应限制在当前文档内并明确保存在 Markdown 中，使重新打开、导出和同步不依赖插件的临时状态。
- 快捷键预览和行内预览应使用局部作用域的定义，确保打开设置页不会向全局注册或重定义宏。
- 校验命令名称、参数引用、重复定义、递归展开以及 `\newcommand` 与 `\renewcommand` 的冲突。
- 每次插入或更新前导定义都应作为一个编辑器事务执行，并可通过一次撤销完整恢复。
- 明确说明：在 Obsidian 之外是否兼容，取决于目标 Markdown 渲染器的 MathJax 配置。

### 非目标

- 不修改未公开的 MathJax 内部实现。
- 不静默安装仓库级全局宏，也不允许一篇笔记改变无关笔记的渲染结果。
- 不依赖其他 Obsidian 插件。

### 验收条件

- 无参数宏和带参数宏在实时预览与阅读视图中表现一致，并能经受打开、编辑、切换笔记和重启 Obsidian。
- 存在 YAML frontmatter 时，宏定义的位置和内容保持稳定。
- 非法或冲突的定义产生可操作的校验提示，且不会部分修改笔记。
- 快捷键预览不会把宏定义泄漏到 Obsidian 共享的 MathJax 状态中。
- 在插件声明的最低 Obsidian 版本上验证桌面端和移动端行为。

对于仅需要提高输入速度的用户，仍优先建议把完整展开后的 LaTeX 表达式作为普通
Math Chords 快捷键插入；当记号会重复出现、包含参数或需要在整篇笔记中统一调整时，
文档级宏仍有不可替代的价值。

## 后续候选

- 为 TikZ 提供可命名的导出预设，包括栅格倍率、背景和文件命名。
- 维护 TikZ 视觉回归样例集，覆盖内置/本机 TeX、明暗主题及中日韩文字。
