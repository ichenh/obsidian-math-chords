# AI 辅助开发说明

[English](AI-ASSISTANCE.md)

本仓库曾使用 Cursor 和大语言模型等 AI 辅助开发工具，目前主要使用 OpenAI Codex 进行维护。这些工具用于辅助实现与审查，不负责项目决策，也不能替代维护者审阅。

## 使用范围

AI 工具可能用于：

- 草拟或重构 TypeScript、测试、构建脚本和文档；
- 识别边界情况并提出测试用例；
- 比较相关代码路径，检查生成文件之间的一致性；
- 协助排查构建、测试、本地化和发布元数据问题。

AI 生成的建议不会仅因能够编译、通过某项测试或来自特定工具而被直接接受。功能范围、架构、兼容性、发布时间以及仓库最终内容均由人工决定。

## 责任与验证

维护者 [CHEN Hua](https://github.com/ichenh) 对项目接受的修改负责。修改需要结合上下文进行审阅，并根据风险程度采取相应验证。仓库的标准检查包括：

- 使用 `npm run build` 进行 TypeScript 检查和生产构建；
- 使用 `npm test` 运行自动化行为测试；
- 使用 `npm run check:shortcuts` 检查生成的快捷键目录是否一致；
- 使用 `npm run check:locales` 检查生成的本地化资源是否完整；
- 使用 `npm run check:release` 检查包信息与发布元数据是否一致；
- 当行为涉及编辑器、命令、设置、快捷键、粘贴处理或撤销历史时，在 Obsidian 中进行手动测试。

`npm run check` 会执行 CI 使用的完整自动化验证序列。

上述检查用于降低风险，但不保证软件不存在缺陷。用户可通过项目的 Issue 跟踪器提交可复现的问题。

## 对贡献者的要求

使用 AI 辅助工具的贡献者必须：

1. 理解并审阅提交的每项修改；
2. 测试受影响的行为，并在可行时补充或更新测试；
3. 在 Pull Request 描述中披露对修改产生实质影响的 AI 辅助；
4. 确认提交内容与项目许可证兼容，且未在未经许可的情况下复用第三方代码或文本；
5. 不向 AI 服务提供密钥、私人仓库内容、个人数据或其他机密信息。

贡献者仍是 Pull Request 的提交者，并对其内容负责。缺少充分审阅、来源说明或验证的 AI 输出可能被拒绝。

## 许可证与透明度

本说明用于记录项目的开发方式，不改变 [MIT 许可证](LICENSE)、插件行为，也不改变用户依据该许可证使用、检查、修改和再分发本软件的权利。

仓库架构、生成文件规则和必要的验证命令见 [AGENTS.md](AGENTS.md)。
